version: v1.0
name: git-tag
agent:
  machine:
    type: s1-prod-ubuntu20-04-amd64-1

blocks:
  - name: "git tag"
    task:
      jobs:
        - name: "git tag"
          commands:
            - checkout
            - git tag $TAG
            - git push orgin $TAG
