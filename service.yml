name: ccloud-sdk-go-v2
lang: go
lang_version: 1.22.12
git:
  enable: true
github:
  enable: true
semaphore:
  enable: true
  pipeline_enable: false
  branches:
    - master
    - /^v\d+\.\d+\.\d+$/
make:
  enable: true
  enable_updates: true
  update_makefile: false
